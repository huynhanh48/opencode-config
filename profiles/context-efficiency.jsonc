{
  "$schema": "https://opencode.ai/config.json",
  // Ecosystem entries for dynamic context pruning / type injection were not
  // available as npm packages at the time this base config was verified.
  // Keep this profile empty unless you replace it with verified package names.
  "plugin": []
}
